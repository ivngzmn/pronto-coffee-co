# Marketing Site

This directory contains the standalone static marketing site for Pronto Coffee Co.

## Local Development

- Run the operations app on `http://localhost:3000`
- Run the marketing site on `http://localhost:4321` with `npm run dev:marketing`

Marketing pages automatically point `Login` and `Register` to the app on port `3000` during local development.
